import { Container } from 'react-bootstrap';
import Header from '../components/Header';

const MainPage = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};
export default MainPage;
